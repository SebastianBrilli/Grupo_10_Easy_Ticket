<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head',{title: 'Todos los eventos'}) %>
  <body>
    <%-include('./partials/header')%>
        <main>
            <section class="eventos">
                <h3>Todos los eventos</h3>

                <div class="products">
                <% for (let i=0; i < event.length; i++) {%>
                <article>
                    <div class="main-article">
                    <img
                        class="img_evento"
                        src="/images/<%= event[i].image%>"
                        width="360"
                        alt="Imagen evento"
                    />
                    <p class="titulo_producto"><%= event[i].name%></p>
                    <p class="description"> <%= event[i].description%> $</p>
                    <a class="info" href="/productDetail/<%=event[i].id%>">
                        Mas informacion ></a>
                    </div>
                </article>
                <%}%>     
                </div>
            </section>
        </main>

    <%-include('./partials/footer')%>
  
</body>
</html>